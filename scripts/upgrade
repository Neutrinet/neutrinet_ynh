#!/bin/bash

#=================================================
# IMPORT GENERIC HELPERS
#=================================================

source _common.sh
source /usr/share/yunohost/helpers

#=================================================
# ENSURE DOWNWARD COMPATIBILITY
#=================================================
ynh_script_progression --message="Ensuring downward compatibility..." --weight=1

if [ ! -d "$install_dir/app" ]; then
    mkdir "$install_dir/app"
    find "$install_dir" -mindepth 1 -maxdepth 1 ! -name "app" -exec mv -t "$install_dir/app" "{}" +
fi

if [[ -n "${opt_path:-}" ]]; then
    ynh_app_setting_delete --app="$app" --key=opt_path
fi

if [[ -n "${app_user:-}" ]]; then
    ynh_app_setting_delete --app="$app" --key=app_user
fi

if [[ -n "${version:-}" ]]; then
    ynh_app_setting_delete --app="$app" --key=version
fi

if [[ -e "/etc/cron.daily/$app-renew-cert" ]]; then
    ynh_secure_remove --file="/etc/cron.daily/$app-renew-cert"
fi

for config_file in /etc/openvpn/client.{conf,cube,ovpn}; do
  if [[ -e "${config_file}" ]]; then
    sed -r -i 's/cipher [a-zA-Z0-9-]+/data-ciphers AES-256-GCM:AES-256-CBC:AES-128-GCM:CHACHA20-POLY1305/g' "${config_file}"
  fi
fi

#=================================================
# REINSTALL STATIC FILES
#=================================================
ynh_script_progression --message="Installing static site..."

ynh_secure_remove --file="$install_dir/app"
cp -r ../sources/. "$install_dir/app"

chmod u+rwX,g+rX,o-rwx "$install_dir"
chown -R "$app:www-data" "$install_dir"

#=================================================
# REINSTALL RENEW CERT
#=================================================
ynh_script_progression --message="Installing VPN certificate renewal tool…"

ynh_setup_source --dest_dir="$renew_cert_path" --full_replace=1

# We wrap the python3 script that actually renew the VPN certificate
# This wrapper will be used as a daily cron task
ynh_add_config --template="renew_cert.sh" --destination="$renew_cert_path/renew_cert.sh"
chmod 755 "$renew_cert_path/renew_cert.sh"
chmod u+rwX,g+rX,o-rwx "$data_dir"
chown -R "$app:www-data" "$data_dir"

#=================================================
# REAPPLY SYSTEM CONFIGURATIONS
#=================================================
ynh_script_progression --message="Upgrading system configurations related to $app..." --weight=1

ynh_add_nginx_config

ynh_add_config --template="renew-cert-cron" --destination="/etc/cron.d/$app-renew-cert"

#=================================================
# FINALIZATION
#=================================================
ynh_script_progression --message="Checking certificates…"

# (This is expected to fail during CI tests because no credential available)
if [[ "${PACKAGE_CHECK_EXEC:-0}" -eq 0 ]]; then
    ynh_exec_warn_less $data_dir/renew_cert/renew_cert.sh
fi

#=================================================
# END OF SCRIPT
#=================================================
ynh_script_progression --message="Installation of $app completed" --last
